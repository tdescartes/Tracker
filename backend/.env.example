DATABASE_URL=postgresql+asyncpg://tracker_user:your_password@localhost:5432/tracker_db
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# OCR — choose one or leave USE_TESSERACT=true for local dev
VERYFI_CLIENT_ID=
VERYFI_CLIENT_SECRET=
VERYFI_USERNAME=
VERYFI_API_KEY=
GOOGLE_CLOUD_VISION_API_KEY=
USE_TESSERACT=true

# Storage (AWS S3 or leave blank to use local disk during dev)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=tracker-receipts
AWS_REGION=us-east-1
USE_LOCAL_STORAGE=true
LOCAL_UPLOAD_DIR=./uploads

# General
APP_NAME=Tracker
DEBUG=true
FRONTEND_ORIGIN=http://localhost:3000

# ── Phase 2 ──────────────────────────────────────
# Recipe suggestions (optional — falls back to built-in recipes if not set)
SPOONACULAR_API_KEY=

# ── Phase 3 ──────────────────────────────────────
# Plaid live bank integration (sandbox for dev, production for live)
# Get credentials at https://dashboard.plaid.com/
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
